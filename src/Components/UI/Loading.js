import { Loader, Spinner } from "../../Styles/LoadingStyle";

const Loading = () => {
  return (
    <Loader>
      <Spinner>
        <div />
        <div />
        <div />
        <div />
      </Spinner>
    </Loader>
  );
};

export default Loading;
